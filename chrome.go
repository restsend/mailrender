package main

func renderWithChrome(url, device, format string) (string, []byte, error) {

	/*
		contentType := "image/png"
		if req.Format == "pdf" {
			contentType = "application/pdf"
		}
	*/
	return "", nil, nil
}
